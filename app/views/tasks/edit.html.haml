.Task-edit
  .task
    %h3.dailytask-name タスクの編集
    = form_with(url: task_path, local: true, method: :PATCH) do |form|
      = form.text_field :task, placeholder: "タスクを変更する", class: 'dailytask-name__text'
      = form.submit "変更", class: "button"