%header.header
  %h1.header__name ToDo App
  %ul
    %li.heder__info
      name:
      = current_user.name
      %ul
        %li
          %a{href: user_path(current_user)} マイページ
        %li
          %a{href: "#"} タスク作成
        %li
          %a{href: edit_user_path(current_user)} アカウント編集

.Task
  .task-group
    %h2.task-groupname グループ名追加

  .task-info
    %h3.dailytask-name 今日のタスク
    
    = form_with(url: tasks_path, local: true, ) do |form|
      = form.text_field :task, placeholder: "タスクを追加する", class: 'dailytask-name__text'
      = form.text_area :detalis, placeholder: "詳細(締め切り日、概要など)"
      = form.submit "追加", class: "button"

    - @tasks.each do |task|
      .content
        .content-main
          .content-name
            担当者:
            = link_to task.user.name, user_path(task.user.id)
          .content-task
            タスク:
            =task.task
          .content-opition
            = link_to '完了', task_path(task.id), method: :delete
            = link_to '編集', edit_task_path(task.id), method: :get
        .content-details
          =task.detalis

  .completetask
    %h2.completetask-name 完了タスク

%footer
  %p Copyright ToDoアプリ 2020.