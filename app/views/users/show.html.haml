.option
  %p
    = @name
    さんのタスク一覧
  - @tasks.each do |task|
    .content
      .content-task
        タスク:
        =task.task
      .content-time
        作成日:
        =task.created_at.strftime('%m/%d')